
connection{
  # Use this keystore for the SSL certificate and key.
  http.keystore = /

  # Specify the keystore password.
  http.keystore.password = 123
}

livy{
  # livy server host
    livy.url = BJSH-DATATEST-199-107
  # livy server port
  livy.port = 8999
}

hadoop{
  #fs.default.name
  uri = nn1.hadoop.test.harbinger.data.m.com/
  # hdfs user
  HADOOP_USER_NAME = hadoopuser

  application.hdfs.dir = /sven_package/

}

